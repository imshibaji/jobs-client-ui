---
import { Applicant } from "@/utils/types/Applicant";
import ApplicantEducations from "./edit/ApplicantEducations";
import ApplicantExperiences from "./edit/ApplicantExperiences";
import ApplicantSkills from "./edit/ApplicantSkills";
import ProfileForm from "./edit/ProfileForm";

const {token, applicant } = Astro.props as { token: string, applicant?: Applicant };
---

<div class="py-2 bg-lavender">
    <div class="space-y-4">
        <div>
            <ProfileForm token={token} applicant={applicant} client:load />
        </div>
        <div class="flex flex-col gap-4 w-full self-stretch">
            <ApplicantSkills token={token} client:load />
            <ApplicantExperiences token={token} client:load />
            <ApplicantEducations token={token} client:load />
        </div>
    </div>
</div>